import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lukajurisic.budgettracker',
  appName: 'Budget Tracker',
  webDir: 'dist'
};

export default config;
